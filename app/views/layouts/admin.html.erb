<!doctype html>
<html>
<head>
  <%= csrf_meta_tags %>
  <%= javascript_include_tag 'admin' %>  
  <title>Painel Administrativo</title>
</head>
<body data-user-id="<%= current_user.udid %>">
  <div class="user">Olá <%= current_user.name %> - <%= link_to 'Sair', destroy_user_session_path, method: :delete, :'data-confirm' => 'Tem certeza que deseja sair?' %><hr></div>
  <%= yield %>
</body>
</html>